import React from "react";

import TopBar from "../top-bar/top-bar";

const Main: React.FC = ({ children }) => {
  return (
    <div>
      main
      <TopBar />
    </div>
  );
};

export default Main;
